import 'package:flutter/material.dart';

class ReservasPerfil extends StatelessWidget {
  const ReservasPerfil({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ReservasPerfil");
  }
}
