import 'package:flutter/material.dart';

class FavoritosPerfil extends StatelessWidget {
  const FavoritosPerfil({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Favoritos"),
        ),
        body: const Text("DSADSA"));
  }
}
