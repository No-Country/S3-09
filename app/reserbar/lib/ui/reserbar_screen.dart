import 'package:flutter/material.dart';

class ReserbarScreen extends StatelessWidget {
  const ReserbarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Home screen");
  }
}
