import React from "react";
import CustomInputForm from "../../components/customInputForm/customInputForm";

const RegisterView = () => {
    return (
        <div>
            {/* TODO: make a similar sing page here but change the useFormik. */}
        </div>
    );
};

export default RegisterView;
